// const files = require.context('./', true, /\.vue$/)

// const m = []
// files.keys().filter(fileKey => {
//   const key = fileKey.slice(2, -4)
//   const m = files(fileKey).default
//   // console.log(module)
//   // console.log(key.replace(/\//g, ''), m)
//   m[key.replace(/\//g, '')] = m
//   // Object.keys(module.state).forEach(k => {
//   //   module.getters[k] = state => state[k]
//   // })
//   // configs[key] = module
// })

// export * from {obj}
